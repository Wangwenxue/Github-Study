git init
git add .
git commit -m "ddd"
git remote add origin xxxxxx
git push -u origin master
user name
password

if exsit fatal: remote origin already , try git remote rm origin

git clone xxxxx
git status
git rm -r folder
git commit -m "ddd"
git push
user name
password
